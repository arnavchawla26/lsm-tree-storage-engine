# lsm-tree-storage-engine
A from-scratch LSM-tree key-value storage engine in Python: skip-list memtable, binary SSTables with bloom filters + sparse index, checksummed WAL crash recovery, size-tiered compaction, and a CLI.
